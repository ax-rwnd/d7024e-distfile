/*
* Kademlia endpoint methods
* NOTE: each message contains a nonce to keep track of messages
* PING - verify that a node is alive
* STORE - store a kv-pair in a node
* FIND_NODE - Recipient returns the k nodes in its own buckets that are closest
*       to the requested key
* FIND_VALUE - Like FIND_NODE, but returns the value from the kv-pair if it is
*       in store
*/

package kademlia
