package main

import (
    "fmt"
)

// Should change package into rest and main() to start(), with input file from Daemon stuff

func main() {    
	fmt.Println("Starting server...")
	initialize() // init.go file
}

